using UnityEngine;

namespace Script.Modules
{
    public class Enemy1Module : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
